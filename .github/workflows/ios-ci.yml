name: iOS CI (Placeholder)

on:
  pull_request:
    branches: [ dev ]
  push:
    branches: [ dev ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder Build
        run: |
          echo "iOS CI placeholder running"
          echo "Replace with Unity build steps when ready"
